// function add(x, y) {
//     return x + y;
// }

//You can define a variable as a function. This is called a function expression

const add = function (x, y) {
    return x + y;
}
// This behaves the same exact way as defining a function not under a variable

// Exercise:

const square = function (x) {
    return Math.pow(x, 2);
};
